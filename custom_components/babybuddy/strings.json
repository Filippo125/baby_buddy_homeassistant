{
  "config": {
    "step": {
      "user": {
        "title": "Set up Baby Buddy",
        "description": "Use Direct mode for a normal Baby Buddy URL. Use Home Assistant Ingress for add-on installs that are only reachable through Home Assistant.",
        "data": {
          "connection_mode": "Connection mode",
          "host": "[%key:common::config_flow::data::host%]",
          "port": "[%key:common::config_flow::data::port%]",
          "path": "[%key:common::config_flow::data::path%]",
          "ingress_token": "Ingress token",
          "api_key": "[%key:common::config_flow::data::api_key%]"
        },
        "data_description": {
          "host": "Direct mode only. Full URL without sub path (for example, https://192.168.1.100).",
          "ingress_token": "Home Assistant Ingress mode only. Token from /api/hassio_ingress/<token>/ URL.",
          "path": "Direct mode only. Optional sub path for Baby Buddy.",
          "port": "Direct mode only. API port for the Baby Buddy instance."
        }
      },
      "reauth_confirm": {
        "description": "The stored api key is no longer valid.",
        "title": "[%key:common::config_flow::title::reauth%]",
        "data": {
          "api_key": "[%key:common::config_flow::data::api_key%]"
        }
      }
    },
    "abort": {
      "reauth_successful": "[%key:common::config_flow::abort::reauth_successful%]",
      "already_configured": "[%key:common::config_flow::abort::already_configured_device%]"
    },
    "error": {
      "cannot_connect": "[%key:common::config_flow::error::cannot_connect%]",
      "invalid_auth": "[%key:common::config_flow::error::invalid_auth%]",
      "missing_fields": "Please complete all required fields for the selected connection mode."
    },
    "selector": {
      "connection_mode": {
        "options": {
          "direct": "Direct URL (host/port/path)",
          "ingress": "Home Assistant Ingress (add-on)"
        }
      }
    }
  },
  "options": {
    "step": {
      "init": {
        "data": {
          "scan_interval": "Update interval (secs)",
          "temperature": "Temperature unit",
          "weight": "Weight unit",
          "feedings": "Feeding amount unit"
        }
      }
    }
  }
}
